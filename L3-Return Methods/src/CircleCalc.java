import javax.swing.*;
import java.text.DecimalFormat;
/*
public class CircleCalc {


    public static void main(String[] args) {

        double radius = getInput ("Please enter the radius");
        double area = area(radius);
        double circumference = circumference(radius);


        public static  double getInput(String message ){
       return Double.parseDouble(JOptionPane.showInputDialog(message));





    }
    public static double area(double radius) {

        return Math.pow(radius, 2)*Math.PI;

    }
    public static double circumference(double radius){

            return 2 * Math.PI * radius;
        }
        public static String displayResults (double area, double perimeter){
            DecimalFormat round = new DecimalFormat("#.##");
            String message = "Tha area is " + round.format(area) + "there circumfernce is " + round.format( circumference);
            return message;
        }
    }






*/
